<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Couche 1 : fond blanc avec coins arrondis -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
            <corners android:radius="15dp" />
        </shape>
    </item>

    <!-- Couche 2 : bandeau bleu à gauche -->
    <item android:width="8dp" android:left="0dp" android:top="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="#50B1F7" />
            <!-- Coins arrondis uniquement à gauche -->
            <corners
                android:topLeftRadius="15dp"
                android:bottomLeftRadius="15dp"
                android:topRightRadius="0dp"
                android:bottomRightRadius="0dp" />
        </shape>
    </item>

</layer-list>